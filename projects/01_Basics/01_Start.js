/*  01_Start.js

Tessel is a micro-controller programmed in Javascript.

Javascript is a programming language most often associated with the Web.
It is one of the three core Web technologies (HTML/CSS/JS), and is increasingly
used in environments that are not run in the browser.

Tessel's runtime environment is Node.js
To learn more about Node.js: https://nodejs.org/api/documentation.html
*/

// The most simple program prints something and exits.
console.log("\nHello Tessel\n")

// This isn't necessary, but calling `return` in the primary scope
// will terminate the program immediately.
return
